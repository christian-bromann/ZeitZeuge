{
  "name": "example",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --hot run src/server.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@zeitzeuge/vitest": "workspace:*",
    "vitest": "^3.1.0"
  }
}
