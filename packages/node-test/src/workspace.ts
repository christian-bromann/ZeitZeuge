/**
 * Workspace builder — re-exports from @zeitzeuge/utils.
 */
export {
  createTestWorkspace as createNodeTestWorkspace,
  mergeHotFunctions,
  type TestWorkspaceResult as NodeTestWorkspaceResult,
} from '@zeitzeuge/utils';
